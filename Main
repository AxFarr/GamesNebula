from tkinter import *
import tkinter.messagebox as tm

import Users
import re
import sqlite3

db = sqlite3.connect('game nebula.db')
c = db.cursor()


class MainFrame(Frame):
    def __init__(self, master):
        Frame.__init__(self, master)

        self.master = master
        self.mainUI()

    def mainUI(self):

        self.master.title("Games Nebula")
        self.pack(fill=BOTH, expand=True)

        frame1 = Frame(self)
        frame1.pack(fill=X)
        frame2 = Frame(self)
        frame2.pack(fill=X)
        frame3 = Frame(self)
        frame3.pack(fill=X)

        self.label_1 = Label(frame1, text="Welcome to Games Nebula")
        self.label_1.pack(side=TOP, padx=5, pady=5)

        self.searchbtn = Button(frame2, text="Browse Games", command = self._search_btn_clickked)
        self.searchbtn.pack(side=LEFT, padx=25, pady=10)
        self.addbtn = Button(frame2, text="Add games", command=self._addbtn_btn_clickked)
        self.addbtn.pack(side=RIGHT, padx=25, pady=10)
        self.updatebtn = Button(frame3, text="Update", command=self._update_btn_clickked)
        self.updatebtn.pack(side=LEFT, padx=40, pady=10)
        self.deletebtn = Button(frame3, text="Delete Games", command=self._delete_btn_clickked)
        self.deletebtn.pack(side=RIGHT, padx=25, pady=10)


    def _search_btn_clickked(self):
        print("Searching")


    def _addbtn_btn_clickked(self):
         print("Adding")

    def _update_btn_clickked(self):
        print("Updating")

    def _delete_btn_clickked(self):
        print("Deleting")


root = Tk()
root.geometry("300x200+300+300")
lf = MainFrame(root)
root.mainloop()
